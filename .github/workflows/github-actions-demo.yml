name: Exécution de add.py

on: [push]

jobs:
  run:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout du code source
      uses: actions/checkout@v2
      
    - name: Exécution de add.py
      run: python add.py
